{
  "name": "server-maps",
  "private": true,
  "dependencies": {
    "firebase-admin": "^8.12.1",
    "puppeteer": "^3.1.0"
  },
  "scripts": {
    "i": "PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true npm install"
  },
  "devDependencies": {
    "@now/node": "^1.6.1"
  }
}
